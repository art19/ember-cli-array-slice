---
language: node_js
node_js:
  - "8.9.4"

sudo: false

cache:
  directories:
    - node_modules

before_install:
  - "npm config set spin false"
  - "npm install -g npm@^6.1.0"

install:
  - npm install -g bower
  - npm install
  - bower install

script:
  - npm test
